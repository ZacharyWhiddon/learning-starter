//This is how you define an Enum!
//Enums are basically lists of constant values.

export enum StatusCodes {
  OK = 200,
  Created = 201,
  Unauthorized = 401,
  Forbidden = 403,
  NotFound = 404,
  ServerError = 500,
}
